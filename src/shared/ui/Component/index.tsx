import React, { FC } from 'react';
import styles from './index.module.scss';

const Component: FC = () => {
    return (
        <div>
            <h2 className={styles.header}>Components</h2>
        </div>
    );
};

export default Component;
